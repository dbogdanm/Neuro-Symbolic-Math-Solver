# Runtime dependencies (direct). Pinned for reproducibility.
# Install with:  pip install -r requirements.txt
Flask==3.1.3
requests==2.32.5
sympy==1.14.0
chromadb==1.5.3
duckduckgo-search==8.1.1
python-dotenv==1.2.2
